
// 1                                                             
  if (( dist5 > objeto)&& (dist4 > objeto)&& (dist3 > objeto) && (dist2 > objeto)&& (dist1 > objeto))
  {
    adelante();
  }
// 2
  if (( dist5 > objeto)&& (dist4 > objeto)&& (dist3 > objeto) && (dist2 > objeto)&& (dist1 <= objeto))
  {
    adelante();
  }
  // 3
  if (( dist5 > objeto)&& (dist4 > objeto)&& (dist3 > objeto) && (dist2 <= objeto)&& (dist1 > objeto))
  {
    izquierda();
  }
   // 4
  if (( dist5 > objeto)&& (dist4 > objeto)&& (dist3 > objeto) && (dist2 <= objeto)&& (dist1 <= objeto))
  {
    izquierda();
  }
   // 5
  if (( dist5 > objeto)&& (dist4 > objeto)&& (dist3 <= objeto) && (dist2 > objeto)&& (dist1 > objeto))
  {
    derecha();
  }
  // 6
  if (( dist5 > objeto)&& (dist4 > objeto)&& (dist3 <= objeto) && (dist2 > objeto)&& (dist1 <= objeto))
  {
    izquierda();
  }
   // 7
  if (( dist5 > objeto)&& (dist4 > objeto)&& (dist3 <= objeto) && (dist2 <= objeto)&& (dist1 > objeto))
  {
    izquierda();
  }
  // 8
  if (( dist5 > objeto)&& (dist4 > objeto)&& (dist3 <= objeto) && (dist2 <= objeto)&& (dist1 <= objeto))
  {
    izquierda();
  }
   // 9
  if (( dist5 > objeto)&& (dist4 <= objeto)&& (dist3 > objeto) && (dist2 > objeto)&& (dist1 > objeto))
  {
    derecha();
  }
   // 10
  if (( dist5 > objeto)&& (dist4 <= objeto)&& (dist3 > objeto) && (dist2 > objeto)&& (dist1 <= objeto))
  {
    izquierda();
  }
  // 11
  if (( dist5 > objeto)&& (dist4 <= objeto)&& (dist3 > objeto) && (dist2 <= objeto)&& (dist1 > objeto))
  {
    derecha();
  }
   // 12
  if (( dist5 > objeto)&& (dist4 <= objeto)&& (dist3 > objeto) && (dist2 <= objeto)&& (dist1 <= objeto))
  {
    izquierda();
  }
  // 13
  if (( dist5 > objeto)&& (dist4 <= objeto)&& (dist3 <= objeto) && (dist2 > objeto)&& (dist1 > objeto))
  {
    izquierda();
  }
   // 14
  if (( dist5 > objeto)&& (dist4 <= objeto)&& (dist3 <= objeto) && (dist2 > objeto)&& (dist1 <= objeto))
  {
    izquierda();
  }
  // 15
  if (( dist5 > objeto)&& (dist4 <= objeto)&& (dist3 <= objeto) && (dist2 <= objeto)&& (dist1 > objeto))
  {
    derecha();
  }
  // 16
  if (( dist5 > objeto)&& (dist4 <= objeto)&& (dist3 <= objeto) && (dist2 <= objeto)&& (dist1 <= objeto))
  {
    izquierda();
  }
  // 17
  if (( dist5 <= objeto)&& (dist4 > objeto)&& (dist3 > objeto) && (dist2 > objeto)&& (dist1 > objeto))
  {
    adelante();
  }
  // 18
  if (( dist5 <= objeto)&& (dist4 > objeto)&& (dist3 > objeto) && (dist2 > objeto)&& (dist1 <= objeto))
  {
    adelante();
  }
  // 19
  if (( dist5 <= objeto)&& (dist4 > objeto)&& (dist3 > objeto) && (dist2 <= objeto)&& (dist1 > objeto))
  {
    derecha();
  }
  // 20
  if (( dist5 <= objeto)&& (dist4 > objeto)&& (dist3 > objeto) && (dist2 <= objeto)&& (dist1 <= objeto))
  {
    atras();
  }
  // 21
    if (( dist5 <= objeto)&& (dist4 > objeto)&& (dist3 <= objeto) && (dist2 > objeto)&& (dist1 > objeto))
    {
      derecha();
    }
    // 22
  if (( dist5 <= objeto)&& (dist4 > objeto)&& (dist3 <= objeto) && (dist2 > objeto)&& (dist1 <= objeto))
  {
    atras();
  }
  // 23
  if (( dist5 <= objeto)&& (dist4 > objeto)&& (dist3 <= objeto) && (dist2 <= objeto)&& (dist1 > objeto))
  {
    derecha();
  }
  // 24
  if (( dist5 <= objeto)&& (dist4 > objeto)&& (dist3 <= objeto) && (dist2 <= objeto)&& (dist1 <= objeto))
  {
    atras();
  }
   // 25
  if (( dist5 <= objeto)&& (dist4 <= objeto)&& (dist3 > objeto) && (dist2 > objeto)&& (dist1 > objeto))
  {
    derecha();
  }
  // 26
  if (( dist5 <= objeto)&& (dist4 <= objeto)&& (dist3 > objeto) && (dist2 > objeto)&& (dist1 <= objeto))
  {
    atras();
  }
  // 27
  if (( dist5 <= objeto)&& (dist4 <= objeto)&& (dist3 > objeto) && (dist2 <= objeto)&& (dist1 > objeto))
  {
    derecha();
  }
   // 28
  if (( dist5 <= objeto)&& (dist4 <= objeto)&& (dist3 > objeto) && (dist2 <= objeto)&& (dist1 <= objeto))
  {
    atras();
  }
  // 29
  if (( dist5 <= objeto)&& (dist4 <= objeto)&& (dist3 <= objeto) && (dist2 > objeto)&& (dist1 > objeto))
  {
    derecha();
  }
  // 30
  if (( dist5 <= objeto)&& (dist4 <= objeto)&& (dist3 <= objeto) && (dist2 > objeto)&& (dist1 <= objeto))
  {
    atras();
  }
  // 31
  if (( dist5 <= objeto)&& (dist4 <= objeto)&& (dist3 <= objeto) && (dist2 <= objeto)&& (dist1 > objeto))
  {
    derecha();
  }
  // 32
  if (( dist5 <= objeto)&& (dist4 <= objeto)&& (dist3 <= objeto) && (dist2 <= objeto)&& (dist1 <= objeto))
  {
    atras();
  }
